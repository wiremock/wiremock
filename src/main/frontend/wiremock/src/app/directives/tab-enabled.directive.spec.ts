import { TabEnabledDirective } from './tab-enabled.directive';

describe('TabEnabledDirective', () => {
  it('should create an instance', () => {
    const directive = new TabEnabledDirective();
    expect(directive).toBeTruthy();
  });
});
