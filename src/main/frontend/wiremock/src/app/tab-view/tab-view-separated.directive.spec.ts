import { TabViewSeparatedDirective } from './tab-view-separated.directive';

describe('TabViewSeparatedDirective', () => {
  it('should create an instance', () => {
    const directive = new TabViewSeparatedDirective();
    expect(directive).toBeTruthy();
  });
});
