import { TabViewRawDirective } from './tab-view-raw.directive';

describe('TabViewRawDirective', () => {
  it('should create an instance', () => {
    const directive = new TabViewRawDirective();
    expect(directive).toBeTruthy();
  });
});
