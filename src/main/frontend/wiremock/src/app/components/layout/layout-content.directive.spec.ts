import { LayoutContentDirective } from './layout-content.directive';

describe('LayoutContentDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutContentDirective();
    expect(directive).toBeTruthy();
  });
});
