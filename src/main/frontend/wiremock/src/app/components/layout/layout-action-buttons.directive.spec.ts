import { LayoutActionButtonsDirective } from './layout-action-buttons.directive';

describe('LayoutActionButtonsDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutActionButtonsDirective();
    expect(directive).toBeTruthy();
  });
});
