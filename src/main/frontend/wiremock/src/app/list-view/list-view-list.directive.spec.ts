import { ListViewListDirective } from './list-view-list.directive';

describe('ListViewListDirective', () => {
  it('should create an instance', () => {
    const directive = new ListViewListDirective();
    expect(directive).toBeTruthy();
  });
});
