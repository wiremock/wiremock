import { ListViewSearchDirective } from './list-view-search.directive';

describe('ListViewSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new ListViewSearchDirective();
    expect(directive).toBeTruthy();
  });
});
