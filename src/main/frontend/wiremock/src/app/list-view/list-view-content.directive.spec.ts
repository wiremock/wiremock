import { ListViewContentDirective } from './list-view-content.directive';

describe('ListViewContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ListViewContentDirective();
    expect(directive).toBeTruthy();
  });
});
