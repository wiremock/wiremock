import { ListViewActionsDirective } from './list-view-actions.directive';

describe('ListViewActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new ListViewActionsDirective();
    expect(directive).toBeTruthy();
  });
});
