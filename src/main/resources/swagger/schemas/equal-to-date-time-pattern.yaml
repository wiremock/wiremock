title: Before datetime
type: object
properties:
  equalToDateTime:
    $ref: "date-time-elements.yaml#/definitions/dateTimeExpression"
  actualFormat:
    $ref: "date-time-elements.yaml#/definitions/format"
  truncateExpected:
    $ref: "date-time-elements.yaml#/definitions/truncation"
  truncateActual:
    $ref: "date-time-elements.yaml#/definitions/truncation"

required:
  - equalToDateTime